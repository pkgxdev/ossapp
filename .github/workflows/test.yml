name: "test"

on:
  push:
    branches:
      - test-again

jobs:
  changes:
    runs-on: macos-latest
    steps:
    - uses: teaxyz/setup@v0
      with:
        version: 0.24.6
    - name: create x86 dmg
      run: |
        tea +github.com/create-dmg/create-dmg create-dmg --version